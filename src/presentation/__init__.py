"""
Presentation layer package enclosing web interfaces and API controllers.

Purpose
-------
Handles HTTP requests, API endpoint routing, request payload validation, response
serialization, and dependency injection for application use cases.

Allowed File Types
------------------
- Web framework setup, API routers, and ASGI application entrypoints.
- Controller endpoint modules, request dependencies, and validation schemas.
"""

"""
Application root package for the DurianPy Badge System backend.

Purpose
-------
Serves as the root module enclosing all layers of the system architecture:
Domain, Application, Infrastructure, Presentation, and Core utilities.
Enforces dependency direction inwards toward the core domain layer.

Allowed File Types
------------------
- Package initialization modules (__init__.py).
- Architecture layer packages (core, domain, application, infrastructure, presentation).
"""

"""
Database persistence infrastructure package.

Purpose
-------
Contains database connection setup, ORM engine configuration, database tables,
and concrete repository implementations satisfying application persistence ports.

Allowed File Types
------------------
- Database engine setup and ORM session connection modules.
- ORM entity model mappings and database table definitions.
- Concrete repository implementations satisfying application ports.
"""

"""
Concrete ORM database models package.

Purpose
-------
Defines persistence structures and table column mappings using ORM frameworks.
Maps database schema fields into internal storage representations.

Allowed File Types
------------------
- ORM table mapping classes (e.g., SQLAlchemy, DynamoDB, SQLModel classes).
- Database schema migration models and index definitions.
- Persistence data mappers and ORM column definitions.
"""

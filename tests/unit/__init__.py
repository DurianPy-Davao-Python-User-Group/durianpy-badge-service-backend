"""
Unit test suite package for isolated component testing.

Purpose
-------
Validates individual domain models, application use cases, infrastructure adapters,
and core utilities in isolation using mocks and stubs.

Directory Structure
-------------------
The directory and file structure within tests/unit/ must mirror the package
hierarchy of src/ (e.g., tests/unit/domain/, tests/unit/application/,
tests/unit/infrastructure/, tests/unit/presentation/, tests/unit/core/).

Allowed File Types
------------------
- Unit test modules corresponding to their mirrored component layers in src/.
- Test double definitions, mock classes, and unit test fixture modules.
"""
